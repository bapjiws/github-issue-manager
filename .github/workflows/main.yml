name: CI

on: 
  issues:
    types: [opened]
  
jobs:
  notify_the_dashboard:
    runs-on: ubuntu-latest
    steps:
      - name: Send a message to the dashboard 
        run: curl https://53e5b645.ngrok.io/issues